# Implementation notes

## Contract questions
(Log ambiguities here instead of improvising. Format: `T<n>: question` - then stop that sub-piece and continue elsewhere.)

## Approved contract additions
(Each task that is allowed to extend SimState says so explicitly in TASKS.md. List them here when made.)

## Tuning log
(T13 only. Format: `param: old → new - why`.)
